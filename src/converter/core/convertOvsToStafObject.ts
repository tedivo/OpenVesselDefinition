import ISectionMapToStafConfig from "../types/ISectionMapToStafConfig";

export default function convertOvsToStafObject<T>(
  data: T,
  sectionConfig: ISectionMapToStafConfig<T>
): string {
  sectionConfig.mapVars;
  //TODO
  return "";
}
